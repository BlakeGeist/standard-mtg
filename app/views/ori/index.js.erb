$('.card-set').append("<%= j render 'templates/cards', :cards => @ori_cards %>");
<% if @ori_cards.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@ori_cards) %>');
<% else %>
  $('.pagination').remove();
<% end %>
