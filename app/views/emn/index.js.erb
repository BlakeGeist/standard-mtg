$('.card-set').append("<%= j render 'templates/cards', :cards => @emn_cards %>");
<% if @emn_cards.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@emn_cards) %>');
<% else %>
  $('.pagination').remove();
<% end %>
