$('.card-set').append("<%= j render 'cards' %>");
<% if @the_cards.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@the_cards) %>');
<% else %>
  $('.pagination').remove();
<% end %>
