<%= form_for(@standard) do |form| %>

  <% if @standard.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@standard.errors.count, "error") %> prohibited
        this stndard from being saved:
      </h2>
      <ul>
        <% @standard.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :active %><br>
    <% unless @standard.active %>
      f <%= form.radio_button :active, false, :checked => true, :value => false %> |
      t <%= form.radio_button :active, true, :checked => false, :value => true %>
    <% else %>
      f <%= form.radio_button :active, false, :checked => false, :value => false %> |
      t <%= form.radio_button :active, true, :checked => true, :value => true %>
    <% end %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>
