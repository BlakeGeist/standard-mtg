$('.card-set').append("<%= j render 'templates/cards', :cards => @cards %>");
<% if @cards.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@cards) %>');
<% else %>
  $('.pagination').remove();
<% end %>
