<%= render '/templates/search' %>

<div>

  <%= get_total(@users_cards) %>

</div>

<div class="card-set">

  <% @cards.each do |card| %>

    <%= render 'dashboards/card', :card => card %>

  <% end %>

</div>

<%= will_paginate @cards %>
