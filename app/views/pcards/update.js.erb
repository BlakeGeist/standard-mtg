$(".pcards").html("<%= escape_javascript(render partial: "cards/pcards", locals: {user: current_user}) %>");

"<%= @pcard = current_user.pcards.find_by! id: params[:id] %>"

$("#div_new_pcard_<%= @pcard.card_id %> .pcard-amount").html("<%= @pcard.amount %>");
