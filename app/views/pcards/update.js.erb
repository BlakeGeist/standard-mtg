$(".pcards").html("<%= escape_javascript(render partial: "cards/pcards", locals: {user: current_user}) %>");

"<%= @pcard = current_user.pcards.find_by! id: params[:id] %>"

//update the amount in the top right corner of the cards
$("#div_new_pcard_<%= @pcard.card_id %> .pcard-amount").html("<%= @pcard.amount %>");
$("#card<%= @pcard.id %> .pcard-amount").html("<%= @pcard.amount %>");
//update the price total

"<%= @users_cards = current_user.pcards.pluck(:card_id, :amount) %>"
$(".collection-total").html("<%= get_total(@users_cards) %>");
