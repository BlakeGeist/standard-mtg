$('.card-set').append("<%= j render 'cards' %>");
<% if @dtk_cards.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@dtk_cards) %>');
<% else %>
  $('.pagination').remove();
<% end %>
